[project]
name = "rich-project"
version = "0.1.0"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/rich_project/__about__.py"

[tool.pytest.ini_options]
testpaths = ["tests"]
